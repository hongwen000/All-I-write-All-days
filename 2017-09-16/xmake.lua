-- define target
target("2017-09-16")

    -- set kind
    set_kind("static")

    -- add files
    add_files("SimpleList.cpp")

-- define target
target("1")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("1.cpp")

    -- add deps
    add_deps("2017-09-16")

-- define target
target("17")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("17.cpp")

    -- add deps
    add_deps("2017-09-16")

-- define target
target("2")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("2.cpp")

    -- add deps
    add_deps("2017-09-16")

-- define target
target("3")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("3.cpp")

    -- add deps
    add_deps("2017-09-16")

-- define target
target("4")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("4.cpp")

    -- add deps
    add_deps("2017-09-16")

-- define target
target("5")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("5.cpp")

    -- add deps
    add_deps("2017-09-16")

-- define target
target("calc")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("calc.cpp")

    -- add deps
    add_deps("2017-09-16")

-- define target
target("con")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("con.cpp")

    -- add deps
    add_deps("2017-09-16")

-- define target
target("fac")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("fac.cpp")

    -- add deps
    add_deps("2017-09-16")

-- define target
target("fjj")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("fjj.cpp")

    -- add deps
    add_deps("2017-09-16")

-- define target
target("hhh")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("hhh.cpp")

    -- add deps
    add_deps("2017-09-16")

-- define target
target("if")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("if.cpp")

    -- add deps
    add_deps("2017-09-16")

-- define target
target("loop")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("loop.cpp")

    -- add deps
    add_deps("2017-09-16")

-- define target
target("main")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("main.cpp")

    -- add deps
    add_deps("2017-09-16")

